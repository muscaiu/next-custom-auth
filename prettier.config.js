module.exports = {
  semi: true, // Use semicolons at the end of statements
  singleQuote: true, // Use single quotes instead of double quotes
  tabWidth: 2, // Set indentation width to 2 spaces
  trailingComma: 'es5', // Add trailing commas where valid in ES5 (objects, arrays, etc.)
  bracketSpacing: true, // Add spaces inside object literals
  jsxBracketSameLine: true, // Keep JSX closing bracket on the same line
  arrowParens: 'always', // Always use parentheses in arrow functions
};
